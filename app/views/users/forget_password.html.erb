<h1>パスワードを再設定します。</h1>

<%= form_tag url: '/reset_password' do |f| %>
  <div class="field">
    <%= label_tag :user_email, 'ELMSメールアドレス' %><br>
    <%= text_field_tag :user_email %>
  </div>

  <div class="field">
    <%= label_tag :user_birthday, '生年月日' %> (本人確認のために利用します。)<br>
    <%= select_date nil, start_year: 1980, end_year: 2000, use_month_numbers: true,
                         prompt: { day: '日', month: '月', year: '年' } %>
  </div>

  <div class="action">
    <%= submit_tag '送信' %>
  </div>
<% end %>
