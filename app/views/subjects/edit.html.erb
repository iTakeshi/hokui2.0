<h1>教科情報を修正</h1>

<%= form_for @subject, url: "/terms/#{@term.term_identifier}/subjects/#{@subject.subject_identifier}/edit", html: { class: 'form-horizontal' } do |f| %>
  <% if @subject.errors.any? %>
    <div id="error_explanation" class="alert alert-block alert-error">
      <a class="close" data-dismiss="alert" href="#">×</a>
      <h4 class="alert-heading"><%= @subject.errors.full_messages.count %>個の項目にエラーがあります。</h4>
      <ul>
        <% @subject.errors.full_messages.each do |msg| %>
          <li><%= slice_error_message(msg) %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <div class="control-group">
      <%= f.label :term_identifier, '学期コード', class: 'control-label' %>
      <div class="controls">
        <%= f.text_field :term_identifier %>
        新学期登録画面と同様に、'2a', '2b'... の形式で学期コードを半角英数字で入力してください。
      </div>
    </div>

    <div class="control-group">
      <%= f.label :subject_identifier, '教科名(英)', class: 'control-label' %>
      <div class="controls">
        <%= f.text_field :subject_identifier %>
        a-z, 0-9, _  以外の文字は使えません。
      </div>
    </div>

    <div class="control-group">
      <%= f.label :subject_name, '教科名(日)', class: 'control-label' %>
      <div class="controls">
        <%= f.text_field :subject_name %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :subject_staff, '教員名', class: 'control-label' %>
      <div class="controls">
        <%= f.text_field :subject_staff %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :subject_lct_cd, '教科コード', class: 'control-label' %>
      <div class="controls">
        <%= f.text_field :subject_lct_cd %>
        シラバスページの「時間割番号」を入力してください。先頭の0も省略せずに入力してください。
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit '送信', class: 'btn btn-primary' %>
    </div>
  </fieldset>
<% end %>
