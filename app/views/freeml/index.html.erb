<h1>freeML 投稿一覧</h1>

クリックすると全文を表示します。
<table class="table">
  <tr>
    <th>投稿ID</th>
    <th>投稿者</th>
    <th>日時</th>
    <th>件名</th>
    <th>本文</th>
  </tr>
  <% @entries.each do |entry| %>
    <tr>
      <td><%= entry.freeml_id %></td>
      <td><%= entry.freeml_user %></td>
      <td><%= entry.freeml_datetime.strftime("%Y/%m/%d %H:%M").gsub('/0', '/') %></td>
      <td><%= entry.freeml_title %></td>
      <td><%= truncate entry.freeml_body %></td>
    </tr>
  <% end %>
</table>
